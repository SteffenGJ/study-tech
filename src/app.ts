export function createAppMarkup(): string {
  return `
    <main class="app-shell">
      <section class="intro">
        <p class="eyebrow">Vite + TypeScript</p>
        <h1>Study Tech</h1>
        <p class="lede">A clean vanilla TypeScript starting point.</p>
      </section>
    </main>
  `
}
